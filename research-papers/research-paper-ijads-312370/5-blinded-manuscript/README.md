# Blinded manuscript — IJADS-312370

`ai67-blinded.pdf` (45 pp) is the revised manuscript with author-identifying
information removed, for blind review.

## What was removed

| Location | Named version | This version |
|---|---|---|
| Running head, every even page | `A. Pardeshi and S. Deshmukh` | empty |
| Title page, author block | names, departments, institutions, e-mails | `Author 1` / `Author 2`, each field "withheld for blind review" |
| "Reference to this paper" line | `Pardeshi, A. and Deshmukh, S.` | `Author~1 and Author~2` |
| Biographical notes | named notes for both authors | withheld, research interests retained |
| Acknowledgements | names both institutions | "withheld to preserve anonymity during review" |

Verified absent from the PDF text layer: `Pardeshi`, `Deshmukh`, `Anandkumar`,
`Sujata`, `fcrit`, `fragnel`, `Rodrigues`, `Vashi`, `Bandra`, `Navi Mumbai`, and every
e-mail address. PDF metadata carries no author or title field.

## What was deliberately kept

**The `Author contributions` block on pp. 40–41 still names both authors**, as
instructed. If the journal requires a fully anonymous file, delete that
`\section*{Author contributions}` block from `ai67-blinded.tex` and recompile; nothing
else needs to change.

## Pagination

This copy paginates **identically** to `2-revised-manuscript/ai67.pdf` — all 49 section
headings and all 26 tables and figures land on the same pages. That is why the author
and biography blocks were redacted in place rather than deleted: it keeps every page
reference in `response-to-reviewers.pdf` valid against either version.

Verified: 45 pp both; 0 section-heading differences; 0 float differences.

## Rebuilding

```bash
cd 5-blinded-manuscript && pdflatex ai67-blinded.tex && pdflatex ai67-blinded.tex
```

`ai67-blinded.tex` is a blinded copy of `2-revised-manuscript/ai67.tex`. It is **not**
regenerated by `3-analysis/assemble.py` — if the manuscript body changes, re-copy
`ai67.tex` here and reapply the five redactions listed above.
